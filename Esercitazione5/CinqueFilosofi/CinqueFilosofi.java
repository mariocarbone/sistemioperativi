package Esercitazione5.CinqueFilosofi;

import java.util.concurrent.Semaphore;

public class CinqueFilosofi {

    Semaphore bacchetta[] = new  Semaphore[5];
//Inizialmente tutti i semafori valgono 1 - Filosofo[i] eseguirà queste operazioni:

    public CinqueFilosofi(){
/*
    while(true){
        bacchetta[i].acquire();
        bacchetta[(i + 1) % 5]).acquire();
        //mangia
        bacchetta[i].release();
        bacchetta[(i + 1) % 5].release();
        //pensa
    }
    }*/
}
}
